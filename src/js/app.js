import log, {hookConsoleLog} from 'stacklogger'
import classNames from 'classnames'

// Model

// View

// Controller

// initialize
function init() {
  try {
    log('init Timer App')
  } catch (e) {
    log(e)
  }
}

hookConsoleLog()
init()
